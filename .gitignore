# Dependencies
node_modules/

# Build artifacts (lib/ is intentionally committed for one-click install;
# only transient client build output is ignored)
lib/.client-build/

# TypeScript
*.tsbuildinfo

# Package output
*.tgz

# Logs and local runtime data
*.log
.DS_Store
Thumbs.db
coverage/
